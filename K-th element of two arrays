
class Solution {
  public:
    int kthElement(vector<int>& a, vector<int>& b, int k) {
    vector<int>arr;
    for(auto i:a)
    arr.push_back(i);
    for(auto i:b)
    arr.push_back(i);
    sort(arr.begin(),arr.end());
    return arr[k-1];
        
        
    }
};
